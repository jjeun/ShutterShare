<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:id="@+id/main">

    <com.astuetz.PagerSlidingTabStrip
        android:id="@+id/tabs"
        app:pstsShouldExpand="true"
        app:pstsTextAllCaps="true"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="@color/sliderBackground"
        android:textSize="20sp"
        android:textColor="@color/sliderFont"
        app:pstsDividerColor="@color/black"
        app:pstsDividerPadding="20dp"
        app:pstsIndicatorColor="@color/tabIndicator"
        app:pstsUnderlineColor="@color/underLine"
        app:pstsTabPaddingLeftRight="14dp"
        android:layout_alignParentBottom="true">

    </com.astuetz.PagerSlidingTabStrip>

    <android.support.v4.view.ViewPager
        android:id="@+id/viewpager"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@color/mainBackground"
        android:layout_above="@+id/tabs" />

</RelativeLayout>

